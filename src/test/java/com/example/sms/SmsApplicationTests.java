package com.example.sms;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class SmsApplicationTests {

	@Test
	void contextLoads() {
		{
			// Пока что нет кода. Этот метод проверяет, загружается ли контекст Spring Boot правильно.
			// Это помогает убедиться, что все конфигурации приложения настроены правильно и контекст может быть успешно запущен.
			// Если контекст не загружается, тест завершится неудачей, указывая на проблемы в конфигурации приложения.
	}

}
